import type { ThemeType } from 'grommet';

const theme: ThemeType = {
  global: {
    font: {
      family: 'Poppins',
    },
    colors: {
      selected: '#00b0aa',
      focus: 'transparent',
    },
  },
};

export default theme;
